import { DailyWeatherDetails } from './daily-weather-details';

describe('DailyWeatherDetails', () => {
  it('should create an instance', () => {
    expect(new DailyWeatherDetails()).toBeTruthy();
  });
});
